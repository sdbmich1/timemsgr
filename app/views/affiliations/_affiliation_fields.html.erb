<div class="fields">
 	<%= f.label :name, "Name" %>  
    <%= f.autocomplete_field :name, autocomplete_organization_OrgName_affiliations_path, :size => 60 %>
   	<%= f.label :affiliation_type, "Type" %> 
   	<%= f.collection_select :affiliation_type, AffiliationType.all, :name, :name, :prompt => true  %> 
	<% if action_name == 'edit' %>
   		<%= f.check_box :_destroy %><%= "Remove" %>
   	<% end %>
</div>