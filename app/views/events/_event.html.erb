<!-- display event time if not an observance event -->
<% if event.event_type == "obsrv" %> 
	<%= render :partial => 'observance_event', :locals => { :event => event } %>
<% else %>
	<%= render :partial => 'general_event', :locals => { :event => event } %>
<% end %>