<% debugger %>
<div style="display:none">
<div id='modal'>
	<p><%= event.title %></p>
	<p><%= " #{event.start_date.strftime("%D")} - #{event.end_date.strftime("%D")}" %><br /> 
	   <%= " #{event.start_time.strftime("%I:%M %p")} - #{event.end_time.strftime("%I:%M %p")}" %></p>
	<p><%= event.description %></p>
	<p><%= event.location %></p>
	<p><%= event.address %></p>
	<p><%= event.city %></p>
</div></div>
