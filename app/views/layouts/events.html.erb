<!DOCTYPE html>
<html>
  <head>	
   		<%= stylesheet_link_tag 'custom', 'fullcalendar', :media => 'screen', :cache => true %>     		
 	  	<%= render 'shared/initialize' %>
 		<script type="text/javascript" src="http://maps.google.com/maps/api/js?sensor=true"></script>
	  	
   		<% if user_signed_in? %>
   			<%= javascript_include_tag 'timer.js', 'slider.js', 'jquery.ticker.js', 'map.js', 'date.js', 'fullcalendar.js', :cache => true %>
   		<% end %> 

		<%= stylesheet_link_tag 'layout', 'links','slider','calendar','dropdown','jquery.fancybox-1.3.4','ticker-style', :media => 'screen', :cache => true %>	
 	  	<%= yield :head %>
  </head>
  <body>
 	<div class="container landing-bg" id="landing">  
 		<div id="spinner" style="display:none"></div>
 		<%= yield :left_sidebar %>
	  	<%= yield :content %>
		<%= yield :scripts %>
		<div id="push"></div>		
	</div>
	<div id="footer" class="container">
		<%= yield :footer %>		
	</div>
	<%= render 'shared/jsheets'%>
    <%= render :partial => 'layouts/ga' if Rails.env.production? %>      
  </body>
</html>
