<% if model %>
  <% for pic in model.pictures %>
	<% if pic %>
   		<%= image_tag pic.photo.url, :class => vtype %>
   	<% end %>		
  <% end %>
  <%= render :partial => 'mobile/photo_item', :locals => {:file_name=>file_name, :model=>model, :psize=>psize, :vtype=>vtype} %>
<% end %>
