<ul data-role="listview">
	<% model.each do |sub| %>
  		<% unless sub.try(:user).blank?  %>
    		<%= render :partial => 'mobile/user_item', :locals => {:model => sub.user} unless connected?(sub.user) %>
  		<% end %>
	<% end %>	
</ul>
