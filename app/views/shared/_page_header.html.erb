	<% if @form == "event_slider" %>		<div id="events"><%= render 'showtime' %></div>		<div class="rpos">				<span class="timeframe"><%= render "dd_daterange" %></span>		</div>	<% else %>		<div class="grp-hdr"><center><%= set_header(@form) %></center></div>		<% if !(@form =~ /event_list/i).nil? %>		  <div class="rpos">				<span class="loc-hdr"><%= link_to "Import", "#modal", :id => "modalGroup", :class=>"edit-btn round nav-right" %></span>  		  </div>  		<% end %>	<% end %>	<hr />