  <% [['speaker', 'Speaker:'], ['speakertopic', 'Topic:'], ['host', 'Host:'],   	  ['RSVPemail', 'RSVP Email:']].each do |fld| %>  	<% unless event.send(fld[0]).blank? %>	  		<div class="span-2"><%= "#{fld[1]}" %></div>  		<div class="span-18 last"><%= event.send(fld[0]) %></div>  	<% end %>  	    <% end %>