<% model.each do |sub| %>
  <% unless sub.try(:user).blank?  %>
    	<%= render :partial => 'shared/user_item', :locals => {:model => sub.user} unless connected?(sub.user) %>
  <% end %>
<% end %>