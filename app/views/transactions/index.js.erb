$('#buyFormError').hide(300);
<% if @order[:qtyCnt].to_i > 0 %>
	$("#buyTxtForm").html("<%= escape_javascript( raw render(:partial => "shared/payment_form") ) %>");
<% else %>
	$('#buyFormError').show(300)
    $('#buyFormError').text('Error submitting form. No quantity amount entered.')
<% end %>
$("#spinner").hide(300);
