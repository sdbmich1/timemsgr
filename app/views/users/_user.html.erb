<%= render 'shared/edit_form' %> <div class="span-20 last round outer-shadow">  	<%= form_for setup_user(@user), :html => { :multipart => true }, :validate => true do |f| %>  		<div class="span-20 last edit-usr pg-title" id="new-event">  			<%= render "shared/error_messages", :target => @user %>  			<%= render :partial => set_area, :locals => { :f => f } %> 		</div>		<p><%= f.submit "Save" %></p>	<% end %>  </div> 