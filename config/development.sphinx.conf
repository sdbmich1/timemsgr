indexer
{
}

searchd
{
  listen = 127.0.0.1:9312
  log = c:/Users/Sean Browm/My Documents/Aptana Studio 3 Workspace/timemsgr/log/searchd.log
  query_log = c:/Users/Sean Browm/My Documents/Aptana Studio 3 Workspace/timemsgr/log/searchd.query.log
  pid_file = c:/Users/Sean Browm/My Documents/Aptana Studio 3 Workspace/timemsgr/log/searchd.development.pid
}

source calendar_event_core_0
{
  type = mysql
  sql_host = localhost
  sql_user = rubyuser
  sql_pass = ruby\#123
  sql_db = kitscentraldb
  sql_sock = /tmp/mysql.sock
  sql_query_pre = SET NAMES utf8
  sql_query_pre = SET TIME_ZONE = '+0:00'
  sql_query = SELECT SQL_NO_CACHE `events`.`ID` * 2 + 0 AS `ID` , `events`.`event_name` AS `event_name`, `events`.`bbody` AS `bbody`, `events`.`cbody` AS `cbody`, `events`.`eventstartdate` AS `eventstartdate`, `events`.`eventenddate` AS `eventenddate`, `events`.`ID` AS `sphinx_internal_id`, 1185062169 AS `class_crc`, 0 AS `sphinx_deleted`, IFNULL(`events`.`event_name`, '') AS `event_name_sort`, IFNULL(`events`.`bbody`, '') AS `bbody_sort`, IFNULL(`events`.`cbody`, '') AS `cbody_sort`, IFNULL(`events`.`eventstartdate`, '') AS `eventstartdate_sort`, IFNULL(`events`.`eventenddate`, '') AS `eventenddate_sort`, `events`.`ID` AS `event_id`, IFNULL(`events`.`event_type`, '') AS `event_type` FROM `events` WHERE (`events`.`ID` >= $start AND `events`.`ID` <= $end AND (status = 'active' AND hide = 'no' AND event_type != 'es')           AND ((eventstartdate >= curdate() )                  OR (eventstartdate <= curdate() and eventenddate >= curdate()) ) ) GROUP BY `events`.`ID`, `events`.`event_name`, `events`.`bbody`, `events`.`cbody`, `events`.`eventstartdate`, `events`.`eventenddate`, `events`.`ID`, `events`.`event_name`, `events`.`bbody`, `events`.`cbody`, `events`.`eventstartdate`, `events`.`eventenddate`, `events`.`ID`, `events`.`event_type` ORDER BY NULL
  sql_query_range = SELECT IFNULL(MIN(`ID`), 1), IFNULL(MAX(`ID`), 1) FROM `events` 
  sql_attr_uint = sphinx_internal_id
  sql_attr_uint = class_crc
  sql_attr_uint = sphinx_deleted
  sql_attr_uint = event_id
  sql_attr_str2ordinal = event_name_sort
  sql_attr_str2ordinal = bbody_sort
  sql_attr_str2ordinal = cbody_sort
  sql_attr_str2ordinal = eventstartdate_sort
  sql_attr_str2ordinal = eventenddate_sort
  sql_attr_str2ordinal = event_type
  sql_query_info = SELECT * FROM `events` WHERE `ID` = (($id - 0) / 2)
}

index calendar_event_core
{
  source = calendar_event_core_0
  path = c:/Users/Sean Browm/My Documents/Aptana Studio 3 Workspace/timemsgr/db/sphinx/development/calendar_event_core
  charset_type = utf-8
}

index calendar_event
{
  type = distributed
  local = calendar_event_core
}

source event_core_0
{
  type = mysql
  sql_host = localhost
  sql_user = rubyuser
  sql_pass = ruby\#123
  sql_db = kitsknndb
  sql_sock = /tmp/mysql.sock
  sql_query_pre = SET NAMES utf8
  sql_query_pre = SET TIME_ZONE = '+0:00'
  sql_query = SELECT SQL_NO_CACHE `events`.`ID` * 2 + 1 AS `ID` , `events`.`event_name` AS `event_name`, `events`.`bbody` AS `bbody`, `events`.`cbody` AS `cbody`, `events`.`eventstartdate` AS `eventstartdate`, `events`.`eventenddate` AS `eventenddate`, `events`.`ID` AS `sphinx_internal_id`, 4201588131 AS `class_crc`, 0 AS `sphinx_deleted`, IFNULL(`events`.`event_name`, '') AS `event_name_sort`, IFNULL(`events`.`bbody`, '') AS `bbody_sort`, IFNULL(`events`.`cbody`, '') AS `cbody_sort`, IFNULL(`events`.`eventstartdate`, '') AS `eventstartdate_sort`, IFNULL(`events`.`eventenddate`, '') AS `eventenddate_sort`, `events`.`ID` AS `event_id`, IFNULL(`events`.`event_type`, '') AS `event_type` FROM `events` WHERE (`events`.`ID` >= $start AND `events`.`ID` <= $end AND (status = 'active' AND hide = 'no' AND event_type != 'es')           AND ((eventstartdate >= curdate() )                  OR (eventstartdate <= curdate() and eventenddate >= curdate()) ) ) GROUP BY `events`.`ID`, `events`.`event_name`, `events`.`bbody`, `events`.`cbody`, `events`.`eventstartdate`, `events`.`eventenddate`, `events`.`ID`, `events`.`event_name`, `events`.`bbody`, `events`.`cbody`, `events`.`eventstartdate`, `events`.`eventenddate`, `events`.`ID`, `events`.`event_type` ORDER BY NULL
  sql_query_range = SELECT IFNULL(MIN(`ID`), 1), IFNULL(MAX(`ID`), 1) FROM `events` 
  sql_attr_uint = sphinx_internal_id
  sql_attr_uint = class_crc
  sql_attr_uint = sphinx_deleted
  sql_attr_uint = event_id
  sql_attr_str2ordinal = event_name_sort
  sql_attr_str2ordinal = bbody_sort
  sql_attr_str2ordinal = cbody_sort
  sql_attr_str2ordinal = eventstartdate_sort
  sql_attr_str2ordinal = eventenddate_sort
  sql_attr_str2ordinal = event_type
  sql_query_info = SELECT * FROM `events` WHERE `ID` = (($id - 1) / 2)
}

index event_core
{
  source = event_core_0
  path = c:/Users/Sean Browm/My Documents/Aptana Studio 3 Workspace/timemsgr/db/sphinx/development/event_core
  charset_type = utf-8
}

index event
{
  type = distributed
  local = event_core
}
